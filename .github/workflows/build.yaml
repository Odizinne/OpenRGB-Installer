name: Build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12.4'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build executable
        run: python src/setup.py build

      - name: Zip OpenRGB-Installer folder
        run: |
          $zipFile = "build/OpenRGB-Installer_win64.zip"
          $folder = "build/OpenRGB-Installer"
          Compress-Archive -Path $folder -DestinationPath $zipFile
        shell: pwsh

      - name: Upload OpenRGB-Installer zip
        uses: actions/upload-artifact@v3
        with:
          name: OpenRGB-Installer
          path: build/OpenRGB-Installer_win64.zip
